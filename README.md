# わたしの家計簿

スマホ向けの個人用家計簿（PWA）。GitHub Pages で公開し、データは自分の Google スプレッドシートに保存します。

- `index.html` … アプリ本体
- `apps-script/Code.gs` … スプレッドシート側の保存用スクリプト（Apps Script にコピーして使う）
- `sw.js` / `manifest.webmanifest` / `icons/` … ホーム画面に追加して全画面で使うための設定

接続先の URL はリポジトリに含めず、各端末の最初の画面で入力します。
